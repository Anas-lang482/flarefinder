"""Stage 9 -- evaluation and uncertainty.

RULE 2: every error metric is reported per flare-size bin. RULE 3: every headline number gets a bootstrap confidence interval.

STATUS: stub. Nothing implemented yet.
All numeric parameters must come from config.yaml (RULE 7).
"""
