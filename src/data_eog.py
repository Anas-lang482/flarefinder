"""Stage 1 -- EOG flare catalogue ingest.

Reads the annual per-site flared volume spreadsheets from data/raw/eog. CAVEAT to carry through every downstream output: EOG volumes are themselves derived from VIIRS, the sensor whose blind spots this project targets.

STATUS: stub. Nothing implemented yet.
All numeric parameters must come from config.yaml (RULE 7).
"""
