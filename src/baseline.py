"""Stage 6 -- VIIRS calibration baseline.

RULE 6: this runs BEFORE anything is built. Reproduces the standard published VIIRS volume calibration and measures its size-dependent bias. Without this there is no measured failure to claim we fixed.

STATUS: stub. Nothing implemented yet.
All numeric parameters must come from config.yaml (RULE 7).
"""
