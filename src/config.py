"""Stage 0 -- configuration loader.

Loads config.yaml and exposes it as a typed object. Single source of truth for every threshold, bound, year and seed in the project.

STATUS: stub. Nothing implemented yet.
All numeric parameters must come from config.yaml (RULE 7).
"""
